<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_marginBottom="@dimen/space_2x"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/iv_task_completed"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_course_item_done"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_task_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/space_2x"
        android:layout_marginTop="@dimen/space_1x"
        android:layout_marginEnd="@dimen/space_3x"
        android:fontFamily="@font/mulish_regular"
        android:text="Выполнил задачу Создать документ"
        android:textColor="@color/black"
        android:textSize="13sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/iv_task_completed"
        app:layout_constraintTop_toTopOf="@id/iv_task_completed" />

    <TextView
        android:id="@+id/tv_task_completion_date"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/space_1x"
        android:text="Февраль 29, 2023."
        android:textSize="11sp"
        app:layout_constraintBottom_toBottomOf="@id/iv_task_completed"
        app:layout_constraintStart_toStartOf="@id/tv_task_text" />

</androidx.constraintlayout.widget.ConstraintLayout>